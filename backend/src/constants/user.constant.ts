export enum UserRoles {
  USER = 'user',
  ADMIN = 'admin',
  DOCTOR = 'doctor',
  BREEDER = 'breeder',
}

export enum AdminRoles {
  SUPER_ADMIN = 'superAdmin',
  ADMIN = 'admin',
}

export enum ProviderTypes {
  GOOGLE = 'google',
  APPLE = 'apple',
}
