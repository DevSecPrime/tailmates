export enum UserRoles {
  USER = 'user',
  ADMIN = 'admin',
}

export enum AdminRoles {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
}

export enum ProviderTypes {
  GOOGLE = 'google',
  APPLE = 'apple',
}
